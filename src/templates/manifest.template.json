{
  "manifest_version": 3,
  "name": "Fingerprint Noise",
  "version": "1.1",
  "description": "Spoofs canvas/WebGL/audio/media fingerprinting from a captured baseline. Per-site exclude and hard-block lists in the toolbar popup.",
  "permissions": ["scripting", "storage", "tabs"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
